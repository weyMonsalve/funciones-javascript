const index = require('./index');

const monedaDolares = index.formatearMoneda(1000, "$");
const monedaEuros = index.formatearMoneda(1000, "€")

console.log(monedaDolares);
console.log(monedaEuros);